#include "main.h"

/**
  * _islower - checks for lowercase character.
  * @c: is the char to be checked
  * Return: 1 if lowercase, 0 otherwise.
  */

int _islower(int c)
{
	if (c >= 97)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
